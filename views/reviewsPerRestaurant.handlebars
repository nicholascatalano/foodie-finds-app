{{!-- <div class='homepage-container'>
  <h2 class='reviews-title'>Review Results for {{this.restaurant}} Below</h2>

  <ul class='nav-list'>
    <li class='nav-item new-post-button'><a href='/new' class='nav-link'>Create
        New Review for
        {{this.restaurant}}</a></li>
  </ul> --}}
<div>
  <p>{{restaurant.name}}</p>
  {{#each reviews}}
    <div class='reviews-section'>
      <ul class='reviews-list'>
        <li>
          <p class='review-rating'>Rating: {{rating}}/5</p>
          <p class='review-content'>{{content}}</p>
        </li>
      </ul>
    </div>
  {{/each}}
</div>